version: "3.8"
services:

  db:
    volumes: 
    - testvolume:/var/lib/mysql
    image: mariadb
    environment:
      MARIADB_ROOT_PASSWORD: example
    ports:
      - 3307:3306
volumes:
  testvolume: 
